#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define M 1000000007
/*
{
    node *curr = head, *prev = NULL, next = NULL;
    while(curr!=NULL)
    {
        forward=curr->next;
        curr->next=prev;
        prev=curr;
        curr=forward;
    }
    head=prev;
}
*/
int main()
{

    return 0;
}